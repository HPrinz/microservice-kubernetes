apiVersion: v1
kind: Pod
metadata:
  labels:
    run: catalog
  name: catalog
spec:
  containers:
  - image: gcr.io/${PROJECT_ID}/${CLUSTERNAME}/microservice-catalog
    name: catalog
    ports:
    - containerPort: 8080